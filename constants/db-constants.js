const DEFAULT_HOST = 'localhost'
const DEFAULT_PORT = 3306
const DEFAULT_DB = 'images'

const DEFAULT_USER = 'root'
const DEFAULT_PASSWORD = '12345'

export {
    DEFAULT_DB,
    DEFAULT_PORT,
    DEFAULT_HOST,
    DEFAULT_USER,
    DEFAULT_PASSWORD
}